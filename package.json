{
  "name": "sstg-project",
  "version": "1.0.0",
  "private": true,
  "description": "Smart School Timetable Generator — monorepo root",
  "workspaces": [
    "frontend"
  ],
  "scripts": {
    "dev": "cd frontend && npm run dev",
    "build": "cd frontend && npm run build",
    "preview": "cd frontend && npm run preview"
  }
}
